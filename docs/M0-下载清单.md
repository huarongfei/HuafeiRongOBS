# M0 环境准备与下载清单（obs 32.2.2 fork）

状态：更新于 M0 阶段。**B1 已完成**；B2/B3 由 CMake 自动下载（官方现代流程），执行前经用户确认。

## B1 ✅ 已做：源码（完成）
- git 浅克隆：obs-studio @ **32.2.2**（HEAD ba2f32bdf791005443988a4955e963663e16b1ed）
- 位置：D:\HuafeirongOBS\obs-src（5162 文件，~200MB）
- 联网方式说明：本机 github.com 直连慢但可达；api.github.com 正常；raw.githubusercontent.com DNS 不通（不用它）。

## B2/B3 ⏳ 待做：预编译依赖 + Qt6（CMake configure 时自动下载）
机制：obs 32.x 在 cmake configure（preset windows-x64）时读取 CMakePresets.json 依赖清单
（cmake/common/buildspec_common.cmake + cmake/windows/buildspec.cmake）自动下载、校验 SHA256、解压到构建目录。

| 依赖 | 版本 | 来源 baseUrl | 量级 |
|---|---|---|---|
| Pre-Built obs-deps（FFmpeg/x264/编解码等） | 2026-07-15 | https://github.com/obsproject/obs-deps/releases/download | ≈1GB |
| Pre-Built Qt6 | 2026-07-15 | 同上（obs-deps releases 托管，无需 qt.io） | ≈400~600MB |
| CEF/Chromium（浏览器源） | 6533 | https://cdn-fastly.obsproject.com/downloads | **关闭**（ENABLE_BROWSER=OFF，省 ~700MB） |

合计预计下载 ≈1.5~2GB（磁盘充足：D 盘空闲 132GB）。
中断处理：CMake 下载完成会写 .dependency_<dep>_<arch>.sha256 标记，重跑 configure 不会重复下载已完成项。

## 校验与安全
- 官方清单内每个依赖带 sha256（CMakePresets.json hashes）→ 由 CMake 强制校验。
- 只从 baseUrl（github obsproject / obs-deps 官方仓库）获取，不引入第三方源。

## M0 剩余步骤（每步前预告；第 1 步会触发大下载）
1. cmake configure（preset windows-x64，ENABLE_BROWSER=OFF）→ 自动下载 B2/B3 并生成构建树
   （预计首次 10~40 分钟，视网速）；
2. 编译（cmake --build / ninja，预计 20~60 分钟）；
3. 运行官方 OBS 冒烟验证（可运行 = M0 完成）；
4. 产出：M1 spike 计划（canvas 渲染集成验证）。

## 备注
- configure/build 需在 VS 开发者环境中执行（VS2026 已装 MSVC 14.51；cmake 4.3.3 已装）。
- 若上游要求 CMake ≥4 已满足；Ninja 已装。
- 锁版策略：obs-src 保持 32.2.2；后续仅小步合入上游安全修复，canvas 相关大面积改动评估后决定。
